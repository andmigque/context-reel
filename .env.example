# ContextReel reads each provider key from the env var named by its model row.
# The browser only ever holds the NAME below, never the value.
# Copy this file to .env and fill in the keys you have.

ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
OPENAI_API_KEY=
XAI_API_KEY=
